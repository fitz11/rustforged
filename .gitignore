/target
/logs
/releases
/library
config.json
*.DS_Store

# Local Apple signing credentials — NEVER commit. These live only here and in
# GitHub Actions secrets; the working-tree copies are for local reference/backup.
.secrets/
*.p12
*.p8

# Local environment file with signing config (sourced by scripts/build-macos.sh).
# .env.example is the committed template; the real .env stays local.
.env
.env.*
!.env.example
